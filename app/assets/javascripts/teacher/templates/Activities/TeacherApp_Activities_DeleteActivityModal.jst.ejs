<div class="modal-content">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h5 class="modal-title" id="myModalLabel" ui-title>Delete <b><%= activity.name %></b>?</h5>
  </div>
  <div class="modal-body" ui-body-div>
    
    <form class="form-horizontal" ui-activity-form>
      <input id="activity_id" name="activity_id" type="hidden" value="<%= activity.id%>">      
      <p>
        Are you sure you want to delete <%= activity.name %>?
      </p>
      <p class="warning_text">
        <% if(student_performance_count > 0){%>
          There are <%= student_count %> students with <%= student_performance_count%> data points.  If you delete this activity, all of these students will lose their data.
        <% } %>
      </p>
    </form>
  </div>
  <div class="modal-footer" ui-footer-div>
    <button type="button" class="btn btn-danger" data-dismiss="modal" ui-delete-button>Delete</button>          
    <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>	        
  </div>
</div>
