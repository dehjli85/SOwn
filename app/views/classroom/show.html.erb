<%= render '/shared/top_nav' %>

<div class="container-fluid">
  <div class="row">  
    <%= render partial: '/shared/left_nav', locals: { active: '' }%>

    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">

      <%= form_for :classroom, url: {action: "create"}, html: {class: "form-horizontal",  method: "post'"} do |f| %>

        
        <div class="form-group">
        
          <label for="inputName" class="col-sm-2 control-label">Name:</label>
          <div class="col-sm-10">      
                <label class="control-label" for="inputClassroomCode"><%= @classroom.name %></label>            
          </div>
        </div>

        <div class="form-group">
        
          <label for="inputName" class="col-sm-2 control-label">Description:</label>
          <div class="col-sm-10">      
                <label class="control-label" for="inputClassroomCode"><%= @classroom.description %></label>            
          </div>
        </div>


        <div class="form-group">
        
          <label for="inputName" class="col-sm-2 control-label">Classroom Code:</label>
          <div class="col-sm-10">      
                <label class="control-label" for="inputClassroomCode"><%= @classroom.classroom_code %></label>            
          </div>
        </div>

        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <a href="/classroom/<%= @classroom.id%>/edit" class="btn btn-default">Edit</a>
          </div>
        </div>
      <% end %>

      <% if !@classroom.activities.empty? %>  
      <a href="/student_performance/edit_all?classroom_id=<%= @classroom.id %>">Edit Data</a>
      <%= form_tag '/' do |f| %>    
          <table id="perf_table" class="table table-condensed table-bordered small" style="table-layout:fixed; width:inherit">
            <thead>
            <tr>
              <th colspan="<%= @classroom.activities.length+1%>" style="text-align:center">Activities</th>            
            </tr>            
            <tr>
              <th style="min-width:150px; vertical-align:bottom">Student</th>
              <% @activites_and_student_performance[:activities].each do |a| %>
                  <th class="activity_header" style=""><%= a[:activity].name %></th>                      
              <% end %>
            </tr>
            </thead>
            <tbody>
            <% @activites_and_student_performance[:student_performances].each do |sp| %>
              <tr>
                <td class=''><%= sp[:student].display_name %></td>        
                <% sp[:performance_array].each_with_index do |p, index| %>

                  <% if @activites_and_student_performance[:activities][index][:activity].activity_type.eql?('scored') %>

                    <td class='<%= !p.nil? ? p.performance_color : "" %>'>
                      <%= !p.nil? ? p.scored_performance_pretty : '' %>                      
                    </td>

                  <% elsif @activites_and_student_performance[:activities][index][:activity].activity_type.eql?('completion') %>

                    <td class="<%= !p.nil? ? p.performance_color : '' %>">
                      <%= !p.nil? ? p.completed_performance_desc : '' %>
                    </td>

                  <% end %>
                  
                <% end %>
              </tr>
            <% end %>
            </tbody>
          </table>
        <% end %>
      <% end %>     
    </div>
  </div>
</div>

<script type="text/javascript">
  $(document).ready(function() 
    { 
        $("#perf_table").tablesorter(); 
    } 
); 
    
</script>

