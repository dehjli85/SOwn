<%= form_for(@student_performance_verification) do |f| %>
  <% if @student_performance_verification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_performance_verification.errors.count, "error") %> prohibited this student_performance_verification from being saved:</h2>

      <ul>
      <% @student_performance_verification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :classroom_activity_pairing_id %><br>
    <%= f.text_field :classroom_activity_pairing_id %>
  </div>
  <div class="field">
    <%= f.label :student_user_id %><br>
    <%= f.number_field :student_user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
